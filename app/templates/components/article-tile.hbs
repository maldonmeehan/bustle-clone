<li>
  {{article.author}} {{article.title}} {{article.category}}
  {{#if isImageShowing}}
    <p><img src={{article.image}} alt={{article.type}} {{action 'imageHide'}}></p>
    {{else}}
      <button {{action 'imageShow'}}>Show image</button>
    {{/if}}
    <button {{action 'delete' rental}}>Delete</button>
</li>
